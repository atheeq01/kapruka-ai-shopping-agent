/**
 * Legacy thin wrapper kept for backward compatibility.
 * New recording UI is handled by RecordingBar + PromptInput.
 * This component is no longer used directly but kept to avoid breaking imports.
 */
export { } from './RecordingBar';
