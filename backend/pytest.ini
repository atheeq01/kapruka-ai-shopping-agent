[pytest]
asyncio_mode = auto
testpaths = tests
addopts = -q --strict-markers
markers =
    smoke: fast boot/health checks that must pass before anything else
    unit: pure-function tests with no network or external services
    api: FastAPI endpoint tests (external Gemini/MCP calls are mocked)
